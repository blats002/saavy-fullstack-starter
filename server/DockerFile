# Build stage
FROM amazoncorretto:21 AS build
COPY . .
RUN chmod +x ./server/gradlew
RUN ./server/gradlew bootJar --no-daemon

FROM amazoncorretto:21 

COPY --from=build /build/libs/*.jar app.jar

EXPOSE 8080

ENTRYPOINT ["java","-jar","app.jar"]
